# ─────────────────────────────────────────────────────────────────────────────
#  Kenya Field Analytics — User Credentials
#  Add / remove users by editing this file. CHANGE THESE PASSWORDS before deploying.
#  On Streamlit Cloud: paste the contents into App Settings → Secrets.
#  Locally: this file lives at .streamlit/secrets.toml  (never commit to git!)
# ─────────────────────────────────────────────────────────────────────────────

[users]
admin   = "changeme-admin"
analyst = "changeme-analyst"

admin_users = ["admin"]
